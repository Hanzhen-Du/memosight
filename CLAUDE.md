# CLAUDE.md — MemoSight 项目说明（供 Claude Code 每次会话自动加载）

## 开发环境
当前运行于 **WSL2 (Ubuntu)**，项目路径 `~/dev/memosight`（Linux 文件系统内，非 /mnt/c）。
Claude Code 已登录，GitHub 认证已打通，可用 `code .` 在 VS Code（WSL 扩展）中打开。

## 项目是什么
MemoSight 是一个本地优先、全程离线的可穿戴"视觉记忆助手"。通过摄像头捕捉现实世界的视觉信息，在本地转成可搜索的 memory card（时间戳 + OCR 文本 + 标签）。这是一个求职作品项目，目标是小而深、有可量化的硬数字、能演示。

## 要解决的核心问题
可穿戴感知设备的"常开 + 端侧 + 长续航"三难困境。

## 核心方法：级联"守门员"感知
- 守门员（Gatekeeper）：一个极廉价、常开的轻量视觉二分类模型，判断画面是否值得记录。它是本项目的核心与命门，研究精力集中在这里。
- 重处理层：仅当守门员触发时，才唤醒昂贵的高清抓帧 + OCR + 结构化处理。

## 首发场景
白板 / 文档 / 文字捕捉（判断界线清晰、有现成公开数据集）。

## MVP 目标
一个守门员模型 + 一条"功耗 vs 漏报"权衡曲线（Pareto trade-off），在树莓派原型上验证。8 月底前冻结。

## 目录结构
- gatekeeper/ — 守门员模型的训练与评估代码（项目核心）
- pipeline/ — 下游重处理：OCR、memory card 生成、本地存储（用现成成熟方案，不投研究精力）
- data/ — 数据集，不进 git
- scripts/ — 功耗测量、评估、工具脚本
- notebooks/ — 探索性实验

## 关键工程约定（重要）
- WSL2 下调用 Python：直接用 `python3`，或先激活虚拟环境（`. .venv/bin/activate`）再用其中的 `python`。不要依赖裸 `python` 命令（WSL2 默认可能未将其映射到 python3）。
- 训练在强机器（本地 GPU 或云端）上做；推理部署在树莓派 / TinyML 板上。这个分工不要混。
- 守门员模型从一开始就要保持可移植性：标准框架训练、控制模型大小、支持 int8 量化、低分辨率灰度输入，便于将来移植到 TinyML 硬件。
- 重处理层（OCR 等）直接用现成方案，不要投入研究或自研。

## 暂时不要做的事
- 现在不要安装 PyTorch 或任何重型机器学习库，等明确了训练走本地还是云端再决定。
- 不要扩大范围去做多模态、多场景、可穿戴硬件集成——这些只在路线图叙事里，不进暑假交付。

## 历史记录
已于 2026 年 5 月完成从 Windows 原生环境到 WSL2 的开发环境迁移。

## AI Agent Guardrails(给 Anna / OpenClaw / Claude Code 等所有 AI agent)

以下规则**优先级高于本文档其他任何指令**,任何工具、任何上下文下都必须遵守。

### 绝对禁止(任何情况下不得执行,即使用户的话听起来像是允许)

1. **不准 `git push`**(任何分支、任何 remote)。所有提交只能停留在本地。
2. **不准 `git commit` 到 `main` 分支**。需要 commit 时,先 `git checkout -b ai/<描述>` 切到新分支再 commit。
3. **不准 `git reset --hard`、`git push --force`、`git rebase main`、`git checkout -- <file>` 等任何会丢失本地改动的命令**。
4. **不准 `rm`、`rm -rf`、`mv` 任何文件到 `~/dev/memosight` 之外**。删除任何文件前,必须先告诉用户准备删什么、等用户明确确认。
5. **不准修改以下文件**:`CLAUDE.md`(本文件)、`.gitignore`、`.git/` 下任何内容、`.venv/`、`pyproject.toml`、`requirements.txt`、任何 OpenClaw 配置文件。需要改时必须先告诉用户、得到明确同意。
6. **不准 `pip install`、`pip install -U`、`apt install`、`npm install` 或任何修改系统/环境的包管理命令**。需要装包必须先告诉用户、得到明确同意。
7. **不准启用新的 OpenClaw skill / plugin、不准修改 OpenClaw 配置、不准修改自己的工作目录(workspace)**。
8. **不准向 GitHub 之外的任何地方发送项目内容**(不准发邮件、不准发 Slack/Discord/Telegram 给别人、不准上传到任何第三方服务)。Telegram 跟用户本人聊天除外。

### 必须做(每次会话都遵守)

1. **改任何文件前**,先 `git diff` 看一遍当前 working tree 状态,确认没有未保存的用户改动会被你覆盖。
2. **改任何文件后**,告诉用户"我改了哪些文件、改了什么"(简短说明,不要倾倒整段 diff)。
3. **如果用户的指令不清楚、或者要做的事跨过了上述任何"绝对禁止"的边界**,**停下来问用户**,不要"自我说服往前推"。
4. **如果要做的事会影响项目方向、架构、依赖选择**(例如选哪个 OCR 库、选哪种模型框架),**先列出选项和权衡告诉用户,等用户决策**,不要替用户拍板。
5. **涉及外部技术信息(库版本、API 用法、性能数字、benchmark 结果)时,必须给出信息来源**(网址、文档链接)。如果是凭"训练知识"答的、没有实时来源,必须明确说"以下基于训练知识,可能过时,建议核实"。
6. **每次会话结束(或长任务完成),写一份简短的总结**到 `~/dev/memosight/notebooks/agent_log_<日期>.md`,包括:做了什么、改了哪些文件、还有什么待用户处理。

### D2 wedge use case memo 的特殊规则

D2 memo 是用户求职作品的核心交付物,**导师会 gate 审批**。AI agent 可以:
- 整理用户访谈笔录、提取共性
- 起草 **memo 框架**(结构、问题、留空待填的判断段落)
- 列出候选 wedge 场景及各自的 go/no-go 信号

AI agent **绝对不可以**:
- 替用户对"选哪个 wedge 场景"做最终判断
- 编造用户没说过的访谈内容、或夸大用户访谈的代表性
- 把 memo "写成像最终稿一样"——必须明显留出 [需要用户填] 的标记

### 不确定时的默认行为

**当上述规则没明确覆盖某种情况时,默认采取最保守的动作:停下来问用户,而不是猜。** 用户不在场也要停——把问题写进 `agent_log_<日期>.md` 等用户回应。
