# MemoSight dependencies
# Install with: pip install -r requirements.txt (inside a virtual environment)

# Data preparation: video frame extraction plus greyscale downscaling
# (scripts/extract_frames.py)
opencv-python

# Data collection: bulk keyword downloads from Pexels (scripts/download_images.py)
requests

# Gatekeeper modelling, training and int8 quantised export
# (scripts/model.py, train.py, export_tflite.py)
# TensorFlow is pinned to 2.19: its prebuilt binaries are compatible with numpy 2.x, but numpy
# is held below 2 for the most stable binaries and for reproducibility.
# The target is TFLite Micro, so the toolchain has to stay TF-native; PyTorch plus ONNX is not
# an option.
tensorflow==2.19.*
numpy<2
# pandas reads train/val/test.csv during training.
pandas
